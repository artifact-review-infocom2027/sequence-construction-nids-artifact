# Metrics

The paper-facing artifact reports Accuracy, Macro-F1, PR-AUC, throughput, and deployment measurements. Public CSV headers and figure labels use the same metric names as the paper.

Temporalization throughput is reported as processed flow records per second for windowed models. Other throughput columns state flows per second in the corresponding CSV or report.

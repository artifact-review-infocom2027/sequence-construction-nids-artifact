# Deployment Benchmark Summary

The deployment evidence is packaged as `evidence/deployment/deployment_benchmark_summary.csv`. It summarizes inference-time measurements for selected representative configurations.

The helper script under `scripts/deployment_benchmark/` reads the packaged summary by default. Checkpoint-based inference benchmarking is a full-scale reproduction activity and requires external model checkpoints and data assets.

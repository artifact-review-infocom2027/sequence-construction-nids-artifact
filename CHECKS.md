# Checks

The repository contains 138 files. Public verification and dry-run commands passed, the claim map points only to repository files, and required public files are present.

Paper-facing figures are stored in `figures/paper/`. Public feature, schema, and label harmonization files are stored in `docs/`.
